(require 'airline-themes)
;;(load-theme 'airline-cool) ;; the airline theme is loaded in custom.el
(setq airline-display-directory 0)
(setq airline-eshell-colors nil)
(setq airline-directory-shortened 1)
(setq airline-shortened-directory-lengh 10)
;; eshell pwd is hided by modifying the source code in airline-themes.el
(provide 'init-airline-themes)
